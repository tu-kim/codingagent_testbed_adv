# Secrets and developer-local overrides only.
# Anything that would normally be committed belongs in deploy/testbed.yaml.

OPENCODE_SERVER_PASSWORD=
HF_TOKEN=

# Dynamo runtime endpoints (consumed as env vars by dynamo.frontend / dynamo.vllm).
NATS_SERVER=nats://127.0.0.1:4222
ETCD_ENDPOINTS=http://127.0.0.1:2379
