{
  "$schema": "https://opencode.ai/config.json",
  "disabled_providers": ["huggingface"],
  "provider": {
    "{{PROVIDER_ID}}": {
      "name": "{{PROVIDER_ID}}",
      "npm": "@ai-sdk/openai-compatible",
      "options": {
        "baseURL": "{{DYNAMO_BASE_URL}}",
        "apiKey": "unused"
      },
      "models": {
        "{{MODEL_SERVED_NAME}}": {
          "name": "{{MODEL_NAME}}"
        }
      }
    }
  },
  "model": "{{PROVIDER_ID}}/{{MODEL_SERVED_NAME}}",
  "permission": { "*": "allow" },
  "agent": {
    "build":      { "temperature": {{TEMPERATURE}}, "top_p": {{TOP_P}}, "options": { "seed": {{SEED}} } },
    "plan":       { "temperature": {{TEMPERATURE}}, "top_p": {{TOP_P}}, "options": { "seed": {{SEED}} } },
    "general":    { "temperature": {{TEMPERATURE}}, "top_p": {{TOP_P}}, "options": { "seed": {{SEED}} } },
    "title":      { "temperature": {{TEMPERATURE}}, "top_p": {{TOP_P}}, "options": { "seed": {{SEED}} } },
    "summary":    { "temperature": {{TEMPERATURE}}, "top_p": {{TOP_P}}, "options": { "seed": {{SEED}} } },
    "compaction": { "temperature": {{TEMPERATURE}}, "top_p": {{TOP_P}}, "options": { "seed": {{SEED}} } }
  }
}
